# Tombola-Java
I nomi e il numero di schede vanno inserite hardcoded, all'avvio vengono generati N numeri finchè il sacchetto non finisce (ArrayList da con numeri da 1 a 90) o finchè la variabile booleana tombola non diventa true
